name: Deploy Development Workflow

on:
  push:
    branches: 
      - dev
  pull_request:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key : ${{ secrets.SERVER_KEY }}
          script: |
            [ ! -d ${{ github.event.repository.name }} ] && git clone --branch dev https://${{ secrets.GIT_USERNAME }}:${{ secrets.GIT_PASSWORD }}@github.com/vasin-peach/itkmitl-tcas-backend
            cd ${{ github.event.repository.name }}
            git checkout dev
            git pull --force
            export APP_PORT=${{ secrets.DEV_APP_PORT }}
            export POSTGRES_USER=${{ secrets.DEV_POSTGRES_USER }}
            export POSTGRES_PASSWORD=${{ secrets.DEV_POSTGRES_PASSWORD }}
            export POSTGRES_PORT=${{ secrets.DEV_POSTGRES_PORT }}
            docker-compose -f docker-compose.dev.yml down
            docker-compose -f docker-compose.dev.yml up -d --no-deps --build

# mkdir -p $HOME/dags_data/linkedin-scapper/screenshort
# mkdir -p $HOME/dags_data/linkedin-scapper/url
# mkdir -p $HOME/dags_data/linkedin-scapper/data
# mkdir -p $HOME/dags_data/linkedin-scapper/cookies
# chmod -R g+rwx $HOME/dags_data/linkedin-scapper
# chown -R vasin:docker $HOME/dags_data/linkedin-scapper
            



